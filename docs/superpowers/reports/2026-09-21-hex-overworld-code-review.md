# 六边形地图更新：提交前独立审查

日期：2026-09-21。审查基线：`958d5ee`；范围为该提交后的工作区地图重构，包括新增文件。三名 Terra 审查者分别负责存档/商店事务、战斗危险适配、地图/界面；主代理核验结论并复跑诊断。

## 审查发现与处理

| 级别 | 问题 | 处理 |
|---|---|---|
| P1 | 血肉造物吞噬的当前生命已包含危险倍率，却进入再次缩放的生命池 | 改为累加不缩放的吸收生命；补真实召唤→逆位→吞噬路径测试 |
| P1 | 召唤物固定生命上限放在临时运行状态，初始化清空后上限错误 | 固定上限转为持久原型配置；真实危险18召唤验证当前生命与上限均为3 |
| P2 | 腐心帷幕无自身咒波来源，却能获得无效的启明词缀 | 核对怪物化术士牌与四张业牌后排除此候选；保留章节权重，新增过滤回归 |
| P2 | 悬停结束后详情及操作按钮没有恢复到选中图格 | 图格退出悬停时通知容器恢复选中详情；真实场景信号回归 |
| P2 | 高价值标记显示到已翻开的正面 | 仅背面绘制，保留持久化的已标记记录 |
| P2 | 强制事件接受未提供的“离开”命令，能解除当前事件锁 | Session 校验该事件实际提供的选项，拒绝伪造或跨事件命令 |
| P2 | 已初始化商店的空库存或畸形商品被视为有效存档 | 补库存结构校验与损坏主档回退诊断 |
| P2 | 不完整 MOVE/SHOP 事务被加载后永久阻塞后续移动 | 补新事务载荷结构校验与兼容备份回退诊断 |
| P3 | 待战斗提示仍包含已移除的“伏击”字样 | 清理旧提示文案 |

合法的“暂时离开、稍后回访”是原有事件的明确选项，不属于跳过未处理事件；保留它，仅拒绝当前事件不提供的选项。冒险旅程诊断也改为实际结算无离开选项的自然恩惠，而不是统一调用 leave。

## 回归证据

审查前的新鲜完整回归：54 项非视觉诊断、3,200 个地图生成组合及真实 GPU 地图视觉检查通过。旧有退出时 Resource/ObjectDB/RID 泄漏告警仍存在，未计作无警告运行。

新增 UI/事件测试先观察到预期失败，再修复到通过。修复后的 UI、Session 与两种子跨章旅程通过；UI 与事件接口补丁已由未实施它们的审查者只读复核。

血肉吞噬、固定生命与存档修复均已由主代理独立核验；吞噬回归采用真实逆位后的力量数值，预期生命上限43、当前生命33，而不是忽略变形属性的错误预期。新增候选过滤断言先观察到失败，随后修复，最后一行过滤补丁也由独立审查者复核通过。

最终验证：54 项非视觉诊断（包含两种子跨章旅程、3,200 个地图生成组合）、真实 GPU 视觉诊断、编辑器导入及暂存内容差异检查均通过。全量回归曾发现旧 adventure_system_check 以 {roll:17} 伪造 MOVE 载荷，被新校验拒绝；已改为调用正式 prepare_move 并额外核对完整载荷 round-trip，重新运行退出0，所有原职业/装备/奖励断言保留。

审查结论：9 项已确认问题均已修复，无未解决的已知提交阻断项；按用户授权提交并正常推送，不使用强制推送。

日志目录：初轮及红绿测试 `/private/tmp/mydeck-review.hcXkNG`；最终修复版本 `/private/tmp/mydeck-review-final.2eDbWx`。冒险总诊断以 `adventure_system_check_rerun.log` 为最终结果，首次失败日志保留。真实画面位于 [hex-overworld](hex-overworld/map_1280.png)。

## 边界

- 不声称审查能证明所有输入下绝无缺陷；交付门槛是无未解决的已确认阻断项，并有新鲜测试证据。
- 本轮未清理历史退出资源泄漏，也未人工完整通关。
- 不改变已批准的事件设计，不扩展到未修改职业卡牌的再次设计审查。
- 推送目标经 fetch 核实为 `origin/master`，非强制推送；推送前本地另有四个既有提交。
