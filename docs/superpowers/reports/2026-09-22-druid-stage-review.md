# 德鲁伊当前阶段审查（2026-09-22）

本阶段已实现并登记灵契／协猎、引灵／赋灵、驱咒祷词／荒魂怒啸；共享双生朝向、区域选择、隐式关闭和 Prayer 延迟打击的审查问题已修复。法脉寄生正位保持“其他角色”限制。

当前顶层玩家卡 74 张，德鲁伊 13 张（基础 3、稀有 8、史诗 2）；完整 78／17 设计中的四张史诗仍未实现。未做视觉渲染声明。

定向验证由 `run-godot.mjs` 执行，覆盖真实逆位界面、候选筛选、弹窗隐式关闭、成功关闭与同步下一次选择、放逐后排队移动使目标离开射程，以及目录归属和资源数量。相关诊断均退出 0。

## 最终审查与验证

- 独立复审确认已修复正逆位配置、双生候选筛选、弹窗生命周期、延后打击合法性、放逐上下文传递和双目录登记问题；没有剩余阻塞项。
- 更新旧法脉寄生诊断：拒绝自身目标且不扣费，选择其他合法角色正常结算；保留新的逐牌限制。
- 最终完整非视觉回归：`HEADLESS_SUITE: 66 passed, 0 failed`，退出码 0。原始日志目录：`/private/tmp/mydeck-precommit-final2-20260922`。
- 执行命令：`sh tools/diagnostics/run_headless_checks.sh <Godot可执行文件> /private/tmp/mydeck-precommit-final2-20260922`。本机版本为 Godot 4.7.2；导入检查与 `git diff --check` 通过。
- 初始牌组保持不变；未执行本阶段的渲染截图验收或 PvE 数值平衡实测。四张后续史诗牌仍待实现。
- 日志仍含 RID、ObjectDB 与资源占用的退出清理警告，未完成对象级归因；测试通过不代表已消除这些警告。
